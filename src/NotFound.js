import React from "react";

/** Not Found - error page rendered for invalid page requests
 *
 * Props:
 * - none
 *
 * State:
 * - none
 *
 * App -> Routes -> NotFound
 */

function NotFound() {
    return (<p>Hmmm. I can't seem to find what you want.</p>);
}

export default NotFound;